version: 2

build:
  tools:
    python: "3.11"

sphinx:
  configuration: docs/source/conf.py

python:
  install:
    - requirements: docs/requirements.txt
```

**Minimal `docs/requirements.txt`:**
```
sphinx>=5.0
sphinx-rtd-theme
